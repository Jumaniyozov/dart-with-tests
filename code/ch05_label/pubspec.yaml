name: ch05_label
description: Study 5 — strings, and what counts as one character.
publish_to: none
resolution: workspace

environment:
  sdk: ^3.13.0

dependencies:
  characters: ^1.4.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
