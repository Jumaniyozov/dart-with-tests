// Study 12 challenges.
//
// Each function below throws. Run `dart test exercises/` to see them fail,
// then make them pass one at a time. Do not edit the tests.
//
// No loops. Every one of these is a chain of the methods from this study, and
// writing it as a loop is the answer study 10 already gave you.

/// 1. The money that went out, written as plain positive amounts:
///    `refunds([100, -250, -5])` reads `['250p', '5p']`. Order is kept.
Iterable<String> refunds(List<int> entries) {
  throw UnimplementedError('challenge 1');
}

/// 2. The total of the busiest day in [week] — the single day whose entries
///    add up to the most. `biggestDay([[10, 10], [50]])` is `50`.
///    An empty week has no busiest day, so answer `0` for one.
int biggestDay(List<List<int>> week) {
  throw UnimplementedError('challenge 2');
}

/// 3. At most three credits over [threshold], in the order they appear:
///    `topUps([100, 500, 20, 700, 900, 1000], 400)` is `[500, 700, 900]`.
List<int> topUps(List<int> entries, int threshold) {
  throw UnimplementedError('challenge 3');
}
