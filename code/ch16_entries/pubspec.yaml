name: ch16_entries
description: Study 16 — a family of types, and a compiler that knows all of them.
publish_to: none
resolution: workspace

environment:
  sdk: ^3.13.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
