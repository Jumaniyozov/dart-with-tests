// Study 3 challenges.
//
// Each function below throws. Run `dart test exercises/` to see them fail,
// then make them pass one at a time. Do not edit the tests.

/// 1. Spaces inside the name become dashes:
///    `slugTag('Hello World')` is `'#hello-world'`.
///    Trimming, lowercasing and the untitled rule must keep working.
String slugTag(String name) {
  throw UnimplementedError('challenge 1');
}

/// 2. Declare a compile-time constant for the longest tag body you allow,
///    set it to 12, and cut anything longer:
///    `shortTag('extraordinarily long')` is `'#extraordina'`.
String shortTag(String name) {
  throw UnimplementedError('challenge 2');
}

/// 3. Count only the characters that are not spaces.
///    `visibleLength('a b c')` is 3.
int visibleLength(String name) {
  throw UnimplementedError('challenge 3');
}
