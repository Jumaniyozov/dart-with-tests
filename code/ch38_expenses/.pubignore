# What a stranger downloading this package does not need.
#
# `dart pub publish --dry-run` reads this and prints the archive it would
# actually upload, which is the only reliable way to find out what you are
# shipping.
test/
exercises/
transcripts/
SLICE
