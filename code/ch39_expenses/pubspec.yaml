name: ch39_expenses
description: Study 39 — the transaction that does not roll back.
version: 5.0.0
repository: https://github.com/Jumaniyozov/dart-with-tests
publish_to: none
resolution: workspace

environment:
  sdk: ^3.13.0

dependencies:
  args: ^2.7.0
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  sqlite3: ^3.5.2

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
