name: ch31_expenses
description: Study 31 — what a report is, and the operators totalling asks for.
publish_to: none
resolution: workspace

environment:
  sdk: ^3.13.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
