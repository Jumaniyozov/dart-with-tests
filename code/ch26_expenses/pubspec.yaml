name: ch26_expenses
description: Study 26 — which failures are data, and which are bugs.
publish_to: none
resolution: workspace

environment:
  sdk: ^3.13.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
