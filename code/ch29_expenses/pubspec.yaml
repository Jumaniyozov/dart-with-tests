name: ch29_expenses
description: Study 29 — a format that can carry what a person types.
publish_to: none
resolution: workspace

environment:
  sdk: ^3.13.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
