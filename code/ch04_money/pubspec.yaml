name: ch04_money
description: Study 4 — int, double, and why money is neither obvious nor a double.
publish_to: none
resolution: workspace

environment:
  sdk: ^3.13.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
