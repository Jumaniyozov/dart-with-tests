// Study 4 challenges.
//
// Each function below throws. Run `dart test exercises/` to see them fail,
// then make them pass one at a time. Do not edit the tests.
//
// Everything here is whole pence. No `double` appears in any answer.

/// 1. Take [percent] off [pence], rounding down to the whole penny.
///    `applyDiscount(1000, 10)` is `900`. A shop never rounds a discount up.
int applyDiscount(int pence, int percent) {
  throw UnimplementedError('challenge 1');
}

/// 2. Write a signed amount: `formatSigned(-150)` is `'-£1.50'` and
///    `formatSigned(150)` is `'£1.50'`. The minus goes before the pound sign,
///    not before the pounds.
String formatSigned(int pence) {
  throw UnimplementedError('challenge 2');
}

/// 3. Round [pence] to the nearest five pence, rounding up at the halfway
///    point: `roundToNearest5(1237)` is `1235` and `roundToNearest5(1238)`
///    is `1240`.
int roundToNearest5(int pence) {
  throw UnimplementedError('challenge 3');
}
