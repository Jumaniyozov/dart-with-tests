name: ch10_ledger
description: Study 10 — the loops and branches the rest of the book was already using.
publish_to: none
resolution: workspace

environment:
  sdk: ^3.13.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
