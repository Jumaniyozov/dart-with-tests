name: ch06_language
description: Study 6 — enums, and letting the compiler close the set.
publish_to: none
resolution: workspace

environment:
  sdk: ^3.13.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
