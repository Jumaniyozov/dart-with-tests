// Study 9 challenges.
//
// Each function below throws. Run `dart test exercises/` to see them fail,
// then make them pass one at a time. Do not edit the tests.
//
// No `!` in any answer. If you reach for it, there is a `??`, a `?.` or an
// `if (x == null)` that does the same job without the crash.

/// 1. Greet [name], or greet a stranger when there is no usable name.
///    `greet('ada')` is `'Hello, ada'`. Nothing, an empty name and a name of
///    only spaces are all strangers.
String greet(String? name) {
  throw UnimplementedError('challenge 1');
}

/// 2. Add up whichever of [typed] are whole numbers, ignoring the rest:
///    `sumTyped(['10', 'x', '5'])` is `15`. An empty list adds to `0`.
int sumTyped(List<String> typed) {
  throw UnimplementedError('challenge 2');
}

/// 3. The first character of [name], or nothing when there is no first
///    character: `initialOf('ada')` is `'a'`, and both `null` and `''` give
///    nothing back.
String? initialOf(String? name) {
  throw UnimplementedError('challenge 3');
}
