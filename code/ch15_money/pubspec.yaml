name: ch15_money
description: Study 15 — a type with a name, methods and a promise.
publish_to: none
resolution: workspace

environment:
  sdk: ^3.13.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
