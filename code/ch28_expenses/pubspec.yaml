name: ch28_expenses
description: Study 28 — the store stops forgetting, and async comes back.
publish_to: none
resolution: workspace

environment:
  sdk: ^3.13.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
