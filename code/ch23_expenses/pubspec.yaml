name: ch23_expenses
description: Study 23 — a boundary the compiler keeps, and where it stops.
publish_to: none
resolution: workspace

environment:
  sdk: ^3.13.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
